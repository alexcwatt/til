# Track without staging

I learned that I can use `git add --intent-to-add path` (or `-N`) to start tracking some new files, without staging their changes.

Then these new files will show up when I `git diff`!
